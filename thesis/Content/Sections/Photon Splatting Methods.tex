Each photon point can also be stored in participating media and projecte d onto the screen as a camera-facing quad disk and a kernel texture similar to the surtrface splat (stuerzringer) is applied upon shading. 9(oudet et.al). The kernell texture construction in this case takesssss account of an integral of kernel fumction evaluation along the thickness of the photon sphere (as seen from the camera). To handle camera-side transmittance, the method ray traced fromeach photon to the camera to accumulate attenuation along the way. Each fragment of the same phone sphere is then assigned the same camera-side transmittance value. This is indeed a good estimation for photonrepresented by point but for photon that are represented by beams, this introduces bias. We willl see in the next section how rasteriztion for photon beam works and later In our method we use a scene overall maps to provide accurate sample of camera-side attenuation value for any position within the view frustrum.
